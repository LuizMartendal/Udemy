CREATE TABLE IF NOT EXISTS contato (
    id CHAR(36) NOT NULL,
    nome VARCHAR(255) NOT NULL,
    email VARCHAR(255) NOT NULL UNIQUE,
    numero INT NOT NULL,
    favorito BOOLEAN,
    PRIMARY KEY (id)
);