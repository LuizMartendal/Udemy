spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/localizacao
    username: root
    password: Mansp25122016*
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
server:
  port: 8080
